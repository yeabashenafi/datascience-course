# These ways can be used to iterate and extract data
# using the index
# using loc() method
# using iloc() method
# using iterrows() method for dataframes
# using the itertuples() method
# using the apply() method uses the lambda function

# Here is how to iterate over the index of the data frame
# for i in octm1.index:
    # print(otcm1['Problem'][i],otcm1[['Medicine'][i]])